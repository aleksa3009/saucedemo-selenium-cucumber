# Base URL for tests
baseUrl=https://www.saucedemo.com/

# Default browser: chrome or firefox
browser=chrome

# Implicit wait time in seconds
implicitWait=10

# Optional: Explicit wait timeout
explicitWait=10
